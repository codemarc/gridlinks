{
  "name": "gridlinks-doc",
  "description": "build and deploy the gridlinks doc site",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "setup": "yarn global add docsify-cli  && docsify init",
    "start": "docsify serve -p 3001 .",
    "deploy": "node deploy.js"
  },
  "dependencies": {
    "shelljs.exec": "^1.1.8"
  }
}
